language: python
python:
  - "2.6"
  - "2.7"
  - "3.2"
  - "3.3"
  - "3.4"
  - "3.5"

# Command to install dependencies
install: "pip install -r requirements.txt"

# Connamd to run tests
script: py.test --verbose
